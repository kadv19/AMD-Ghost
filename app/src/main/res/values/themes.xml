<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MediaPipeApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Main theme colors -->
        <item name="colorPrimary">@color/ghost_accent</item>
        <item name="android:windowBackground">@color/ghost_bg_deep</item>
        <item name="android:statusBarColor">@color/ghost_bg_deep</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/ghost_bg_deep</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o">false</item>
    </style>

    <style name="Theme.MediaPipeApp" parent="Base.Theme.MediaPipeApp" />
</resources>
